{
  "name": "towerfanreviews",
  "type": "module",
  "version": "1.0.0",
  "description": "towerfanreviews.uk — UK tower fan reviews, comparisons and buying guides",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler pages deploy dist --project-name towerfanreviews --branch master --commit-dirty=true",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.1.0",
    "tailwindcss": "^4.0.0"
  }
}
